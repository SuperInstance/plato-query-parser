[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"
[project]
name = "plato-query-parser"
version = "0.1.0"
description = "Query intent classification, keyword extraction, domain detection"
license = {text = "MIT"}
requires-python = ">=3.8"
